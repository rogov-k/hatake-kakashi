{
  "buildCommand": "echo 'Skipping TypeScript build — already built'",
  "outputDirectory": ".",
  "builds": [
    { "src": "/dist/api/**/*.js", "use": "@vercel/node" },
    { "src": "/dist/index.html", "use": "@vercel/static" }
  ],
  "routes": [
    { "src": "/dist/(.*)", "dest": "/dist/api/$1" },
    { "src": "/dist/api/(.*)", "dest": "/dist/api/$1.js" }
  ]
}
