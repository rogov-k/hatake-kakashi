{
  "functions": {
    "dist/api/**/*.js": {
      "runtime": "nodejs20.x"
    }
  },
  "builds": [
    { "src": "dist/api/**/*.js", "use": "@vercel/node" }
  ],
  "ignoreCommand": "echo 'Ignoring non-dist files'"
}
